[project]
name = "viva-engage-rescue"
version = "0.1.0"
description = "Skrapar och arkiverar Viva Engage-communities till sökbart SQLite-arkiv"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
